export const REPORT_STATUS_ENUM = {
    resolved: 'resolved',
    rejected: 'rejected',
} as const;

export const REPORT_SEVERITY_ENUM = {
    severe: 'severe',
    moderate: 'moderate',
    low: 'low',
    untagged: 'untagged',
} as const;
